/*
 * Late API
 * API reference for Late. Authenticate with a Bearer API key. Base URL: https://getlate.dev/api 
 *
 * The version of the OpenAPI document: 1.0.1
 * Contact: support@getlate.dev
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package dev.getlate.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import dev.getlate.model.Connected;
import dev.getlate.model.ConnectedAccount;
import dev.getlate.model.Expired;
import dev.getlate.model.Pending;
import java.time.OffsetDateTime;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import dev.getlate.ApiClient;
import dev.getlate.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-03-13T13:25:23.838967082Z[Etc/UTC]", comments = "Generator version: 7.19.0")
@JsonDeserialize(using = CompleteTelegramConnect200Response.CompleteTelegramConnect200ResponseDeserializer.class)
@JsonSerialize(using = CompleteTelegramConnect200Response.CompleteTelegramConnect200ResponseSerializer.class)
public class CompleteTelegramConnect200Response extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CompleteTelegramConnect200Response.class.getName());

    public static class CompleteTelegramConnect200ResponseSerializer extends StdSerializer<CompleteTelegramConnect200Response> {
        public CompleteTelegramConnect200ResponseSerializer(Class<CompleteTelegramConnect200Response> t) {
            super(t);
        }

        public CompleteTelegramConnect200ResponseSerializer() {
            this(null);
        }

        @Override
        public void serialize(CompleteTelegramConnect200Response value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class CompleteTelegramConnect200ResponseDeserializer extends StdDeserializer<CompleteTelegramConnect200Response> {
        public CompleteTelegramConnect200ResponseDeserializer() {
            this(CompleteTelegramConnect200Response.class);
        }

        public CompleteTelegramConnect200ResponseDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public CompleteTelegramConnect200Response deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize Connected
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (Connected.class.equals(Integer.class) || Connected.class.equals(Long.class) || Connected.class.equals(Float.class) || Connected.class.equals(Double.class) || Connected.class.equals(Boolean.class) || Connected.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((Connected.class.equals(Integer.class) || Connected.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((Connected.class.equals(Float.class) || Connected.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (Connected.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (Connected.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(Connected.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'Connected'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'Connected'", e);
            }

            // deserialize Expired
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (Expired.class.equals(Integer.class) || Expired.class.equals(Long.class) || Expired.class.equals(Float.class) || Expired.class.equals(Double.class) || Expired.class.equals(Boolean.class) || Expired.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((Expired.class.equals(Integer.class) || Expired.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((Expired.class.equals(Float.class) || Expired.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (Expired.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (Expired.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(Expired.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'Expired'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'Expired'", e);
            }

            // deserialize Pending
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (Pending.class.equals(Integer.class) || Pending.class.equals(Long.class) || Pending.class.equals(Float.class) || Pending.class.equals(Double.class) || Pending.class.equals(Boolean.class) || Pending.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((Pending.class.equals(Integer.class) || Pending.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((Pending.class.equals(Float.class) || Pending.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (Pending.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (Pending.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(Pending.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'Pending'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'Pending'", e);
            }

            if (match == 1) {
                CompleteTelegramConnect200Response ret = new CompleteTelegramConnect200Response();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for CompleteTelegramConnect200Response: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public CompleteTelegramConnect200Response getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "CompleteTelegramConnect200Response cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public CompleteTelegramConnect200Response() {
        super("oneOf", Boolean.FALSE);
    }

    public CompleteTelegramConnect200Response(Connected o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public CompleteTelegramConnect200Response(Expired o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public CompleteTelegramConnect200Response(Pending o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Connected", Connected.class);
        schemas.put("Expired", Expired.class);
        schemas.put("Pending", Pending.class);
        JSON.registerDescendants(CompleteTelegramConnect200Response.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CompleteTelegramConnect200Response.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * Connected, Expired, Pending
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(Connected.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(Expired.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(Pending.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Connected, Expired, Pending");
    }

    /**
     * Get the actual instance, which can be the following:
     * Connected, Expired, Pending
     *
     * @return The actual instance (Connected, Expired, Pending)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Connected`. If the actual instance is not `Connected`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Connected`
     * @throws ClassCastException if the instance is not `Connected`
     */
    public Connected getConnected() throws ClassCastException {
        return (Connected)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Expired`. If the actual instance is not `Expired`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Expired`
     * @throws ClassCastException if the instance is not `Expired`
     */
    public Expired getExpired() throws ClassCastException {
        return (Expired)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Pending`. If the actual instance is not `Pending`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Pending`
     * @throws ClassCastException if the instance is not `Pending`
     */
    public Pending getPending() throws ClassCastException {
        return (Pending)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof Pending) {
        if (getActualInstance() != null) {
          joiner.add(((Pending)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof Connected) {
        if (getActualInstance() != null) {
          joiner.add(((Connected)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof Expired) {
        if (getActualInstance() != null) {
          joiner.add(((Expired)getActualInstance()).toUrlQueryString(prefix + "one_of_2" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

